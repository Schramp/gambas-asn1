// Generated by asn1cpp-validation-tools/xval_sweep/xval_sweep.py — registers
// the single PDU type this target sweeps, same pattern as
// etsitools/asn1cpp/randgen/etsi_types.cpp (that file is hand-written
// because the ETSI schema only ever needed two fixed types; this one is
// machine-generated per target since the PDU type varies per schema).
// Assumes __PDU_TYPE__ is a class-generating kind (SEQUENCE/CHOICE/SET/
// ENUMERATED — every real message PDU type is one of these), so
// `__PDU_TYPE__::asn_DEF` resolves. A free-standing alias (raw `asn_DEF_X`,
// no class) isn't a realistic PDU root and isn't supported by this
// template — see xval_sweep/targets.txt's own doc on what qualifies.
#include "type_registry.hpp"
#include "__PDU_TYPE__.hpp"

namespace {
struct Registrar {
    Registrar() {
        randgen::register_types({
            randgen::make_entry<__PDU_TYPE__>("__PDU_TYPE__", __PDU_TYPE__::asn_DEF),
        });
    }
} registrar;
} // anonymous namespace
