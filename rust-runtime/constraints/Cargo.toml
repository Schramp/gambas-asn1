[package]
name = "asn1cpp-constraints"
version = "0.1.0"
edition = "2021"
description = "Thin re-export shim over asn1cpp-wire::constraints (gambas-asn1#537) — kept for external path compatibility."

[lib]
name = "asn1cpp_constraints"
path = "src/lib.rs"

[dependencies]
asn1cpp-wire = { path = "../wire" }
