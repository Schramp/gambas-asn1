[package]
name = "asn1cpp-per"
version = "0.1.0"
edition = "2021"
description = "Native Rust PER (unaligned) codec primitives — no FFI to the C++ runtime."

[lib]
name = "asn1cpp_per"
path = "src/lib.rs"
