[package]
name = "etsi-li-ps-pdu-rust-probe"
version = "0.1.0"
edition = "2021"
publish = false
description = "gambas-asn1#299: best-effort --target=rust build of the real ETSI LI PS-PDU schema, tracking progress toward full Rust codegen coverage. Expected to fail until #300-#305 (and further construct-coverage issues) land. Generated by `make rust-probe` in the parent directory — not wired into ctest/CI."

[lib]
path = "src/lib.rs"

[dependencies]
asn1cpp-ber = { path = "../../../rust-runtime/ber" }
